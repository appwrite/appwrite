# appwrite-locale

Appwrite locale strings and helpers.

This crate is a **stub**. It will carry Appwrite-specific translations and locale helpers built on top of `utopia-locale` where useful.

## Install

```toml
appwrite-locale = { workspace = true }
```

## Status

Placeholder `Locale` type only. Not production-ready.
