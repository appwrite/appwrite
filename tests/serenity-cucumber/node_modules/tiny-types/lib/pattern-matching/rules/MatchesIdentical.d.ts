import { MatcherRule } from './MatcherRule';
/**
 * @access private
 */
export declare class MatchesIdentical<Input_Type, Output_Type> extends MatcherRule<Input_Type, Output_Type> {
    private readonly pattern;
    constructor(pattern: Input_Type, transformation: (v: Input_Type) => Output_Type);
    matches(value: Input_Type): boolean;
}
