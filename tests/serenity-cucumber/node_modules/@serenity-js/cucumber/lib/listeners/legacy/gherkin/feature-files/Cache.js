"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Cache = void 0;
const errors_1 = require("./errors");
/**
 * @private
 */
class Cache {
    store;
    constructor(store = {}) {
        this.store = store;
    }
    set(key, value) {
        this.store[this.idOf(key)] = value;
    }
    get(key) {
        if (!this.store[this.idOf(key)]) {
            throw new errors_1.UnableToRetrieveFeatureFileMap(`Make sure you cache a value under ${key.toString()} before trying to retrieve it`);
        }
        return this.store[this.idOf(key)];
    }
    has(key) {
        return !!this.store[this.idOf(key)];
    }
    size() {
        return Object.keys(this.store).length;
    }
    clear() {
        this.store = {};
    }
    idOf(key) {
        return key.toString();
    }
}
exports.Cache = Cache;
//# sourceMappingURL=Cache.js.map