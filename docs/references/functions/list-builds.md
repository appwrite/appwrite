Get a list of all the current user build logs. You can use the query params to filter your results. If accessed using a server SDK or an API key with the relevant scopes, this endpoint will return a list of all of the project's builds regardless of the permissions that have been set for the function.