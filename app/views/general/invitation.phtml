<?php
$projectName = $this->getParam('projectName', 'Appwrite');
$teamName = $this->getParam('teamName', '');
$redirectUrl = $this->getParam('redirectUrl', '');
$membershipId = $this->getParam('membershipId', '');
$teamId = $this->getParam('teamId', '');
$userId = $this->getParam('userId', '');
$secret = $this->getParam('secret', '');
$callbackBase = $this->getParam('callbackBase', '');
?>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/svg+xml" href="<?php echo htmlspecialchars($callbackBase); ?>/images/logos/appwrite-icon.svg" />
    <title>Join <?php echo htmlspecialchars($teamName); ?> - Appwrite</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Outfit:wght@500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --color-bg: #09090b;
            --color-card: rgba(18, 18, 20, 0.7);
            --color-border: rgba(255, 255, 255, 0.08);
            --color-text-primary: #f4f4f5;
            --color-text-secondary: #a1a1aa;
            --color-primary: #fd366e;
            --color-primary-hover: #e0245e;
            --color-success: #10b981;
            --color-error: #ef4444;
        }

        @media (prefers-color-scheme: light) {
            :root {
                --color-bg: #f8f9fa;
                --color-card: rgba(255, 255, 255, 0.9);
                --color-border: rgba(0, 0, 0, 0.06);
                --color-text-primary: #18181b;
                --color-text-secondary: #71717a;
                --color-primary: #fd366e;
                --color-primary-hover: #e0245e;
            }
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            background-color: var(--color-bg);
            font-family: 'Inter', sans-serif;
            color: var(--color-text-primary);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 24px;
            overflow-x: hidden;
        }

        .bg-glow {
            position: absolute;
            width: 600px;
            height: 600px;
            background: radial-gradient(circle, rgba(253, 54, 110, 0.08) 0%, rgba(0, 0, 0, 0) 70%);
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            z-index: -1;
            pointer-events: none;
        }

        .card {
            background: var(--color-card);
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);
            border: 1px solid var(--color-border);
            border-radius: 24px;
            padding: 40px;
            width: 100%;
            max-width: 480px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
            text-align: center;
            animation: fadeIn 0.6s cubic-bezier(0.16, 1, 0.3, 1) forwards;
            position: relative;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .logo {
            height: 48px;
            margin-bottom: 32px;
            display: inline-block;
        }

        .title {
            font-family: 'Outfit', sans-serif;
            font-size: 28px;
            font-weight: 700;
            line-height: 1.25;
            margin-bottom: 12px;
            letter-spacing: -0.02em;
        }

        .subtitle {
            font-size: 15px;
            color: var(--color-text-secondary);
            line-height: 1.6;
            margin-bottom: 32px;
        }

        .team-highlight {
            color: var(--color-text-primary);
            font-weight: 600;
        }

        .button-group {
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .btn {
            display: inline-flex;
            justify-content: center;
            align-items: center;
            height: 48px;
            padding: 0 24px;
            border-radius: 12px;
            font-size: 15px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s ease;
            text-decoration: none;
            border: none;
            outline: none;
            width: 100%;
        }

        .btn-primary {
            background-color: var(--color-primary);
            color: #ffffff;
            box-shadow: 0 4px 12px rgba(253, 54, 110, 0.2);
        }

        .btn-primary:hover {
            background-color: var(--color-primary-hover);
            transform: translateY(-1px);
        }

        .btn-primary:active {
            transform: translateY(1px);
        }

        .btn-secondary {
            background-color: transparent;
            color: var(--color-text-secondary);
            border: 1px solid var(--color-border);
        }

        .btn-secondary:hover {
            color: var(--color-text-primary);
            background-color: rgba(255, 255, 255, 0.03);
        }

        @media (prefers-color-scheme: light) {
            .btn-secondary:hover {
                background-color: rgba(0, 0, 0, 0.02);
            }
        }

        .redirect-notice {
            font-size: 12px;
            color: var(--color-text-secondary);
            margin-top: 24px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 6px;
        }

        .redirect-notice svg {
            width: 14px;
            height: 14px;
            fill: currentColor;
        }

        .spinner {
            display: none;
            width: 20px;
            height: 20px;
            border: 2px solid rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            border-top-color: #fff;
            animation: spin 0.8s linear infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        .error-message {
            display: none;
            background-color: rgba(239, 68, 68, 0.1);
            border: 1px solid rgba(239, 68, 68, 0.2);
            color: var(--color-error);
            padding: 12px 16px;
            border-radius: 12px;
            font-size: 14px;
            margin-bottom: 24px;
            text-align: left;
            align-items: center;
            gap: 10px;
        }

        .success-message {
            display: none;
            color: var(--color-success);
            font-size: 16px;
            font-weight: 500;
            margin-bottom: 24px;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }

        .success-message svg {
            width: 20px;
            height: 20px;
            fill: currentColor;
        }
    </style>
</head>
<body>
    <div class="bg-glow"></div>
    <div class="card">
        <img class="logo" src="<?php echo htmlspecialchars($callbackBase); ?>/images/logos/appwrite.svg" alt="Appwrite Logo" />
        
        <div id="errorBox" class="error-message">
            <svg viewBox="0 0 24 24" width="20" height="20" fill="currentColor">
                <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-2h2v2zm0-4h-2V7h2v6z"/>
            </svg>
            <span id="errorText">An error occurred. Please try again.</span>
        </div>

        <div id="successBox" class="success-message">
            <svg viewBox="0 0 24 24" width="20" height="20" fill="currentColor">
                <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
            </svg>
            <span>Invitation accepted! Redirecting...</span>
        </div>

        <h1 class="title">Join Invitation</h1>
        <p class="subtitle">
            You've been invited to join the team <span class="team-highlight"><?php echo htmlspecialchars($teamName); ?></span> in project <span class="team-highlight"><?php echo htmlspecialchars($projectName); ?></span>.
        </p>

        <div class="button-group">
            <button id="acceptBtn" class="btn btn-primary" onclick="acceptInvitation()">
                <span id="btnText">Accept Invitation</span>
                <div id="btnSpinner" class="spinner"></div>
            </button>
            <a id="declineBtn" class="btn btn-secondary" href="<?php echo htmlspecialchars($redirectUrl); ?>">
                Decline
            </a>
        </div>

        <div class="redirect-notice">
            <svg viewBox="0 0 24 24">
                <path d="M18 19H6c-.55 0-1-.45-1-1V6c0-.55.45-1 1-1h5c.55 0 1-.45 1-1s-.45-1-1-1H6c-1.66 0-3 1.34-3 3v12c0 1.66 1.34 3 3 3h12c1.66 0 3-1.34 3-3v-5c0-.55-.45-1-1-1s-1 .45-1 1v5c0 .55-.45 1-1 1zm-3-14c-.55 0-1 .45-1 1s.45 1 1 1h2.59l-9.13 9.13c-.39.39-.39 1.02 0 1.41.39.39 1.02.39 1.41 0L19 8.41V11c0 .55.45 1 1 1s1-.45 1-1V5c0-.55-.45-1-1-1h-6z"/>
            </svg>
            <span>You will be redirected to <?php echo htmlspecialchars(parse_url($redirectUrl, PHP_URL_HOST)); ?></span>
        </div>
    </div>

    <script>
        async function acceptInvitation() {
            const acceptBtn = document.getElementById('acceptBtn');
            const declineBtn = document.getElementById('declineBtn');
            const btnText = document.getElementById('btnText');
            const btnSpinner = document.getElementById('btnSpinner');
            const errorBox = document.getElementById('errorBox');
            const errorText = document.getElementById('errorText');
            const successBox = document.getElementById('successBox');

            errorBox.style.display = 'none';
            
            acceptBtn.disabled = true;
            declineBtn.style.pointerEvents = 'none';
            btnText.style.display = 'none';
            btnSpinner.style.display = 'inline-block';

            const teamId = "<?php echo htmlspecialchars($teamId); ?>";
            const membershipId = "<?php echo htmlspecialchars($membershipId); ?>";
            const userId = "<?php echo htmlspecialchars($userId); ?>";
            const secret = "<?php echo htmlspecialchars($secret); ?>";
            const redirectUrl = "<?php echo htmlspecialchars($redirectUrl); ?>";

            try {
                const response = await fetch(`/v1/teams/${teamId}/memberships/${membershipId}/status`, {
                    method: 'PATCH',
                    headers: {
                        'Content-Type': 'application/json',
                        'x-appwrite-project': '<?php echo htmlspecialchars($this->getParam("project") ? $this->getParam("project")->getId() : ""); ?>'
                    },
                    body: JSON.stringify({
                        userId: userId,
                        secret: secret
                    })
                });

                if (!response.ok) {
                    const errorData = await response.json();
                    throw new Error(errorData.message || 'Failed to accept the invitation.');
                }

                successBox.style.display = 'flex';
                acceptBtn.style.display = 'none';
                declineBtn.style.display = 'none';

                setTimeout(() => {
                    window.location.href = redirectUrl;
                }, 1500);

            } catch (err) {
                acceptBtn.disabled = false;
                declineBtn.style.pointerEvents = 'auto';
                btnText.style.display = 'inline';
                btnSpinner.style.display = 'none';

                errorText.innerText = err.message;
                errorBox.style.display = 'flex';
            }
        }
    </script>
</body>
</html>
